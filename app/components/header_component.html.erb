<div class="pb-5 mb-5 border-b border-gray-200 sm:flex sm:items-center sm:justify-between">
  <div class="sm:w-0 sm:flex-1">
    <h1 id="message-heading" class="text-lg font-medium text-gray-900"><%= @title %></h1>
    <% if @description.present? %>
      <p class="mt-1 text-sm text-gray-500 overflow-hidden overflow-ellipsis"><%= @description %></p>
    <% end %>
  </div>

  <div class="mt-3 flex sm:mt-0 sm:ml-4">
    <%= content %>
  </div>
</div>
