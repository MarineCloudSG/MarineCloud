<aside class="py-6 px-2 sm:px-6 lg:py-0 lg:px-0 lg:col-span-3" data-controller="tab-navigation-component">
  <nav class="space-y-1" >
    <% links.each do |link| %>
      <%= link %>
    <% end %>
  </nav>
</aside>

<%= turbo_frame_tag @turbo_frame_tag, src: @initial_tab_src, class: "lg:col-span-9" %>
